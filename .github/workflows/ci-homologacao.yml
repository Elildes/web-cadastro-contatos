name: CI/CD - Homologação (Baseline rc)

on:
  pull_request:
    branches:
      - homologacao
    types:
      - closed

jobs:
  build_and_tag:
    if: github.event.pull_request.merged == true 
    name: Criar Baseline (rc-x.x)
    runs-on: ubuntu-latest

    steps:
      - name: Checkout do repositório
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Configurar Git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Determinar e Criar Nova Tag (rc-x.x)
        env:
          SOURCE_BRANCH: ${{ github.event.pull_request.head.ref }}
        run: |
          LATEST_TAG=$(git tag -l "rc-*" | sort -V | tail -n 1)
          
          if [ -z "$LATEST_TAG" ]; then
            MAJOR=1
            MINOR=0
          else
            if [[ $LATEST_TAG =~ rc-([0-9]+)\.([0-9]+) ]]; then
              MAJOR="${BASH_REMATCH[1]}"
              MINOR="${BASH_REMATCH[2]}"
            else
              MAJOR=1
              MINOR=0
            fi
          fi

          if [[ "$SOURCE_BRANCH" == "main" ]]; then
            MAJOR=$((MAJOR + 1))
            MINOR=0
          elif [[ "$SOURCE_BRANCH" == bugfix/* ]]; then
            MINOR=$((MINOR + 1))
          fi

          NEW_TAG="rc-${MAJOR}.${MINOR}"
          git tag -a "$NEW_TAG" -m "Baseline gerada via PR da branch $SOURCE_BRANCH"
          git push origin "$NEW_TAG"